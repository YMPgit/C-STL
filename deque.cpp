#include <bits/stdc++.h>
using namespace std;

int main(){

    //same functions as others 
    //but we cannot access index in this
    deque<int>d={1,2,3,4,5};

    for(int val:d){
        cout<<val<<" ";
    }
}